<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".GlobalSettings"
        android:theme="@style/AppTheme">
        <activity android:name=".highrisk"></activity>
        <activity android:name=".mediumriskActivity" />
        <activity android:name=".lowriskActivity" />
        <activity android:name=".noriskActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".ScopeActivity" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".WhatisActivity" />
        <activity android:name=".Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>